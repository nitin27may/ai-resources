# Pinned deliberately. Material for MkDocs warns that MkDocs 2.0 removes the
# plugin system and rewrites theming with no migration path, so the major
# version is capped rather than left to float on every CI run.
#
# Only the plugins mkdocs.yml actually enables are listed. Adding a package
# here without enabling it in mkdocs.yml just slows CI down.
mkdocs>=1.6,<2
mkdocs-material>=9.7,<10
pymdown-extensions>=11,<12

# Enabled in mkdocs.yml: glightbox, git-revision-date-localized, redirects.
# `search` and `tags` ship with MkDocs and Material respectively.
mkdocs-glightbox>=0.5,<1
mkdocs-git-revision-date-localized-plugin>=1.5,<2

# Not yet used. Present so that any page move lands with its redirect in the
# same commit rather than waiting on a dependency change.
mkdocs-redirects>=1.2,<2
